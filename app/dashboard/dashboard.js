'use strict';

angular.module('myApp').controller('DashboardCtrl', function($scope){
 // selectedRepo comes from previous scope
 // ToDo Expose selectedRepo via event (and update based on that event)

});